{
  "name": "trustlens-backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cross-env PORT=4000 nodemon --watch src --exec node src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "mysql2": "^3.2.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.22"
  }
}
